const screens=[...document.querySelectorAll('.screen')];
const nav=[...document.querySelectorAll('.bottom-nav button')];
const localState=JSON.parse(localStorage.getItem('ascendPathState')||'{"practiceDays":0,"entries":[]}');
let user=null,entitlement=null,curriculum=null,currentStage=null,currentPractice=null,progressRow=null,markerObservations=[],recentJournalText='',libraryQuery='',libraryType='all';

function saveLocal(){localStorage.setItem('ascendPathState',JSON.stringify(localState));renderCounts(localState.practiceDays)}
function renderCounts(days){document.getElementById('practice-days').textContent=days;document.getElementById('profile-days').textContent=days}
function setSync(text,online=false){const el=document.getElementById('sync-state');el.textContent=text;el.classList.toggle('online',online)}
function showAccount(){const signed=!!user;document.getElementById('account-signed-out').classList.toggle('hidden',signed);document.getElementById('account-signed-in').classList.toggle('hidden',!signed);if(signed)document.getElementById('account-email').textContent=user.email||'ASCEND student'}
function ensureEnterButton(){let button=document.getElementById('enter-ascend');if(button)return button;button=document.createElement('button');button.id='enter-ascend';button.type='button';button.className='primary hidden';button.textContent='Enter ASCEND Path';button.addEventListener('click',()=>window.ASCENDUX?.activateScreen?.('today')||nav.find(item=>item.dataset.screen==='today')?.click());document.getElementById('sign-out')?.insertAdjacentElement('beforebegin',button);return button}
function applyAccessGate(hasAccess){document.body.classList.toggle('auth-required',!user);document.body.classList.toggle('access-required',!!user&&!hasAccess);ensureEnterButton()?.classList.toggle('hidden',!user||!hasAccess);if(user&&!hasAccess)window.ASCENDUX?.activateScreen?.('me')}
async function refreshAccess(){const label=document.getElementById('access-level'),form=document.getElementById('lifetime-key-form'),message=document.getElementById('access-message'),purchase=document.getElementById('paywall-purchase');if(!label||!user){entitlement=null;applyAccessGate(false);return false}try{const [fetchedEntitlement,teacher]=await Promise.all([PathBackend.getMyEntitlement(user.id),PathBackend.isTeacher(user.id).catch(()=>false)]);entitlement=fetchedEntitlement;const paid=PathBackend.entitlementIsActive(entitlement);const active=paid||teacher;const lifetime=paid&&entitlement.access_level==='lifetime';const premium=paid&&entitlement.access_level==='premium';label.textContent=teacher&&!paid?'Teacher Access':lifetime?'Lifetime Access':premium?'Premium Access':'Access Required';label.classList.toggle('lifetime',lifetime);label.classList.toggle('premium',premium||(teacher&&!paid));form?.classList.toggle('hidden',active);purchase?.classList.toggle('hidden',active);if(message)message.textContent=active?(teacher&&!paid?'Teacher standing grants full access to review students’ shared work.':'Your full ASCEND Path is active.'):'ASCEND Path requires an active Premium membership or a Lifetime tester key.';applyAccessGate(active);return active}catch(e){console.error(e);entitlement=null;label.textContent='Access Required';form?.classList.remove('hidden');purchase?.classList.remove('hidden');if(message)message.textContent='Access could not be verified. Try again before entering the Path.';applyAccessGate(false);return false}}
function escapeHtml(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}

nav.forEach(b=>b.addEventListener('click',()=>{if(window.ASCENDUX)return;nav.forEach(x=>x.classList.remove('active'));b.classList.add('active');screens.forEach(s=>{const active=s.id===b.dataset.screen;s.classList.toggle('active',active);if(!active)s.classList.remove('motion-enter');s.setAttribute('aria-hidden',String(!active))})}));

function stagePractice(stage){if(!curriculum||!stage)return null;const link=curriculum.links.find(l=>l.stage_id===stage.id&&l.role==='primary');return link?curriculum.practices.find(p=>p.id===link.practice_id):null}
function progressFor(stage){return window.__pathProgress?.find(p=>p.stage_id===stage.id)}
function stageMarkers(){return curriculum?.markers?.filter(m=>m.stage_id===currentStage?.id)||[]}

function renderStage(){if(!currentStage)return;currentPractice=stagePractice(currentStage);const p=currentPractice;
  window.curriculum=curriculum;window.currentStage=currentStage;
  document.getElementById('stage-eyebrow').textContent='CORE FORMATION';
  document.getElementById('stage-title').textContent=p?.title||currentStage.title;
  document.getElementById('profile-stage').textContent=currentStage.title;
  document.getElementById('practice-name').textContent=`${p?.default_minutes||10} min`;
  document.getElementById('overlay-practice-title').textContent=p?.title||'Practice';
  document.getElementById('overlay-practice-instructions').textContent=p?.instructions||currentStage.objective||'';
  document.getElementById('briefing-title').textContent=p?.title||'Practice';
  document.getElementById('briefing-intention').textContent=p?.instructions||currentStage.objective||'';
  document.getElementById('briefing-duration').textContent=`${p?.default_minutes||10} minutes`;
  document.getElementById('briefing-begin').textContent=`Begin ${p?.default_minutes||10}-Minute Practice`;
  document.getElementById('stage-requirement').textContent=`${currentStage.required_practice_days} required`;
  const days=progressRow?.practice_days??localState.practiceDays;renderCounts(days);document.getElementById('stage-day').textContent=`DAY ${Math.max(1,days+1)}`;const journeyDay=document.getElementById('journey-now-day');if(journeyDay)journeyDay.textContent=`Day ${Math.max(1,days+1)}`;window.PathTheme?.apply?.();
  remaining=(p?.default_minutes||10)*60;drawTimer();renderPath();renderLibrary();renderStageReview();window.ASCENDProgression?.invalidate?.();document.dispatchEvent(new CustomEvent('ascend:curriculum'));
}

function renderPath(){if(!curriculum)return;const list=document.getElementById('path-list');list.innerHTML='';curriculum.stages.forEach(stage=>{const li=document.createElement('li');const row=progressFor(stage);const status=stage.id===currentStage?.id?'current':(row?.status==='established'?'done':'');if(status)li.classList.add(status);const phase=curriculum.phases.find(p=>p.id===stage.phase_id);let label='Later on the Path';if(stage.id===currentStage?.id)label='Current stage';else if(status==='done')label='Established';else if(row?.status==='review')label='In review';else if(phase?.gate_label)label=phase.gate_label;li.innerHTML=`<span></span><div><strong>${escapeHtml(stage.title)}</strong><small>${escapeHtml(label)}</small></div>`;list.appendChild(li)});const li=document.createElement('li');li.className='future';li.innerHTML='<span></span><div><strong>Further stages</strong><small>Revealed as the curriculum unfolds</small></div>';list.appendChild(li)}

function contentCardHTML(item){return `<small>${escapeHtml(item.content_type.toUpperCase())}</small><strong>${escapeHtml(item.title)}</strong><span>${escapeHtml(item.summary||'Available now')}</span>`}
function contentAccess(item){const rules=(curriculum?.contentRules||[]).filter(rule=>rule.content_id===item.id);if(!rules.length)return{unlocked:true,label:'Available now'};const currentOrder=Number(currentStage?.sort_order)||1;const required=rules.map(rule=>curriculum.stages.find(stage=>stage.id===rule.stage_id)).filter(Boolean).sort((a,b)=>a.sort_order-b.sort_order)[0];return{unlocked:!required||currentOrder>=Number(required.sort_order),label:required?`Opens at ${required.title}`:'Opens later on the Path'}}
function createLibraryCard(item){const access=contentAccess(item),a=document.createElement('article');a.className='content-card'+(access.unlocked?'':' locked');if(item.slug)a.dataset.slug=item.slug;if(access.unlocked){a.setAttribute('role','button');a.setAttribute('tabindex','0');a.setAttribute('aria-label',`Open ${item.title}`)}else{a.setAttribute('aria-disabled','true');a.setAttribute('tabindex','-1')}a.innerHTML=access.unlocked?contentCardHTML(item):`<small>LATER</small><strong>${escapeHtml(item.title)}</strong><span>${escapeHtml(access.label)}</span>`;return a}
function renderLibrary(){if(!curriculum)return;
  const list=document.getElementById('library-list'),count=document.getElementById('library-count');list.innerHTML='';
  const query=libraryQuery.toLowerCase(),typed=curriculum.content.filter(item=>libraryType==='all'||item.content_type===libraryType),visible=typed.filter(item=>!query||[item.title,item.summary,item.content_type].some(value=>String(value||'').toLowerCase().includes(query)));
  const grouped=!query&&libraryType==='all';
  if(grouped){
    const groups=[['teaching','Teachings'],['practice','Practices'],['reading','Readings'],['reference','References']];
    groups.forEach(([type,label])=>{const items=visible.filter(item=>item.content_type===type);if(!items.length)return;const details=document.createElement('details');details.className='library-group';const summary=document.createElement('summary');summary.innerHTML=`<span>${label}</span><small>${items.length}</small>`;const itemsWrap=document.createElement('div');itemsWrap.className='library-group-items';items.forEach(item=>itemsWrap.appendChild(createLibraryCard(item)));details.append(summary,itemsWrap);list.appendChild(details)});
  }else visible.forEach(item=>list.appendChild(createLibraryCard(item)));
  if(!visible.length){list.innerHTML='<div class="empty-state"><h2>No matching teaching</h2><p>Try another word or content type.</p></div>'}
  if(count)count.textContent=`${visible.length} of ${curriculum.content.length} Library items`;
  const rail=document.getElementById('library-recommended'),listLabel=document.getElementById('library-list-label');
  let hasPicks=false;
  if(rail){
    rail.innerHTML='';
    const unlocked=curriculum.content.filter(item=>contentAccess(item).unlocked);
    if(!libraryQuery&&libraryType==='all'&&unlocked.length&&window.LibraryEngine){
      const picks=window.LibraryEngine.recommend(unlocked,{history:window.LibraryEngine.loadLibraryHistory(),journalText:recentJournalText,n:3});
      if(picks.length){
        hasPicks=true;
        const eyebrow=document.createElement('div');eyebrow.className='eyebrow';eyebrow.textContent='RECOMMENDED FOR YOU';rail.appendChild(eyebrow);
        picks.forEach(item=>{const a=document.createElement('article');a.className='content-card';if(item.slug)a.dataset.slug=item.slug;a.setAttribute('role','button');a.setAttribute('tabindex','0');a.setAttribute('aria-label',`Open ${item.title}`);a.innerHTML=contentCardHTML(item);rail.appendChild(a)});
      }
    }
  }
  if(listLabel){listLabel.textContent=grouped?'BROWSE LIBRARY':'RESULTS';listLabel.classList.remove('hidden')}
}

function renderStageReview(){const card=document.getElementById('stage-review-card'),markers=stageMarkers();if(!currentStage||currentStage.progression_mode==='time'){card.classList.add('hidden');return}card.classList.remove('hidden');let reviewImage=document.getElementById('stage-review-image');if(!reviewImage){reviewImage=document.createElement('div');reviewImage.id='stage-review-image';reviewImage.className='stage-review-image hidden';card.querySelector('.eyebrow')?.insertAdjacentElement('afterend',reviewImage)}const seasonalImages=currentStage.metadata?.seasonal_images;if(Array.isArray(seasonalImages)&&seasonalImages.length){const dayPick=Math.floor((Date.now()-Date.UTC(new Date().getUTCFullYear(),0,0))/864e5)%seasonalImages.length;reviewImage.style.setProperty('background-image',`linear-gradient(180deg,rgba(4,18,27,0) 45%,rgba(4,18,27,.92)),url("assets/seasonal-art/${seasonalImages[dayPick]}")`,'important');reviewImage.classList.remove('hidden')}else{reviewImage.classList.add('hidden')}document.getElementById('review-title').textContent=`${currentStage.title} · Reflection`;const list=document.getElementById('marker-list');list.innerHTML='';if(!markers.length)list.innerHTML='<p class="review-intro">Readiness for this stage is assessed from practice consistency, elapsed time, journal reflection and application in ordinary life. No separate attainment checklist is required.</p>';markers.forEach(marker=>{const existing=markerObservations.find(o=>o.marker_id===marker.id);const block=document.createElement('div');block.className='marker-card';block.innerHTML=`<div class="marker-type">${escapeHtml(marker.marker_type==='steiner'?'TRADITIONAL MARKER':marker.marker_type==='ascend'?'ASCEND MARKER':marker.marker_type.replace('_',' ').toUpperCase())}</div><strong>${escapeHtml(marker.title)}</strong><p>${escapeHtml(marker.description||'Observe this quality without forcing a conclusion.')}</p>${marker.attribution?`<small>${escapeHtml(marker.attribution)}</small>`:''}<label class="visually-hidden" for="marker-${marker.id}">Readiness state for ${escapeHtml(marker.title)}</label><select id="marker-${marker.id}" data-marker-id="${marker.id}"><option value="not_yet">Not yet</option><option value="occasionally">Occasionally</option><option value="increasingly">Increasingly</option><option value="established">Clearly established</option></select><label class="visually-hidden" for="marker-note-${marker.id}">Observed evidence for ${escapeHtml(marker.title)}</label><textarea id="marker-note-${marker.id}" data-marker-reflection="${marker.id}" placeholder="What have you actually observed?">${escapeHtml(existing?.reflection||'')}</textarea>`;const select=block.querySelector('select');select.value=existing?.state||'not_yet';select.addEventListener('change',()=>saveMarker(marker.id,select.value,block.querySelector('textarea').value));block.querySelector('textarea').addEventListener('change',e=>saveMarker(marker.id,select.value,e.target.value));list.appendChild(block)});const days=progressRow?.practice_days||0;const canRequest=days>=currentStage.required_practice_days;const button=document.getElementById('submit-stage-review');button.disabled=!canRequest||!user;button.textContent=canRequest?'Request Progression Review':`${currentStage.required_practice_days-days} Practice Days Remaining`;document.getElementById('review-status').textContent=progressRow?.status==='review'?'Your stage is in review. Continue practicing while the review remains open.':'';setTimeout(()=>window.ASCENDReadinessEvidence?.load?.(),80)}

async function saveMarker(markerId,state,reflection){if(!user||!currentStage)return;try{setSync('SYNCING…');await PathBackend.saveMarkerObservation(user.id,currentStage.id,markerId,state,reflection);const old=markerObservations.find(o=>o.marker_id===markerId);if(old){old.state=state;old.reflection=reflection}else markerObservations.push({marker_id:markerId,state,reflection});setSync('SYNCED',true)}catch(e){console.error(e);document.getElementById('review-status').textContent='Could not save this reflection yet.';setSync('LOCAL')}}

async function refreshMirror(){const box=document.getElementById('mirror-content');if(!user||!currentStage){box.innerHTML='<p>Sign in and begin journaling to create a grounded reflection.</p>';return}if(window.ASCENDMirror?.load){await window.ASCENDMirror.load();return}box.innerHTML='<p>Reading your recent journal…</p>'}

async function loadRemote(){try{user=await PathBackend.me();showAccount();window.__pathProgress=[];recentJournalText='';markerObservations=[];curriculum=null;currentStage=null;progressRow=null;if(!user){entitlement=null;applyAccessGate(false);setSync('SIGN IN');return}setSync('CHECKING ACCESS…');const hasAccess=await refreshAccess();if(!hasAccess){setSync('LOCKED');return}setSync('LOADING…');curriculum=await PathBackend.loadCurriculum();const profile=await PathBackend.ensureStudent(user);window.__pathProgress=await PathBackend.getProgress(user.id);recentJournalText=await PathBackend.getRecentJournalText(user.id).catch(()=>'');progressRow=window.__pathProgress.find(p=>p.status==='active'||p.status==='review')||window.__pathProgress[window.__pathProgress.length-1]||null;currentStage=curriculum.stages.find(s=>s.id===progressRow?.stage_id)||curriculum.stages[0]||null;if(currentStage)markerObservations=await PathBackend.getMarkerObservations(user.id,currentStage.id);renderStage();await refreshMirror();window.ASCENDBranches?.load?.();window.ASCENDTrainingLayers?.load?.();window.ASCENDReadinessEvidence?.load?.();setSync('SYNCED',true);window.ASCENDIntro?.consider?.({currentUser:user,profile})}catch(e){console.error(e);setSync('UNAVAILABLE');showAccount();applyAccessGate(false)}}

const overlay=document.getElementById('practice-overlay'),briefing=document.getElementById('practice-briefing');function openPracticeOverlay(){briefing.classList.remove('hidden')}function beginPractice(){briefing.classList.add('hidden');resetTimerUI();overlay.classList.remove('hidden')}window.ASCENDOpenPractice=openPracticeOverlay;document.querySelector('[data-action="practice"]').addEventListener('click',openPracticeOverlay);document.getElementById('briefing-begin').addEventListener('click',beginPractice);document.querySelector('.briefing-close').addEventListener('click',()=>briefing.classList.add('hidden'));document.querySelector('#practice-overlay .overlay-close').addEventListener('click',()=>overlay.classList.add('hidden'));
let remaining=600,interval=null,running=false;const timer=document.getElementById('timer'),toggle=document.getElementById('timer-toggle'),finishBtn=document.getElementById('finish-practice'),timerHint=document.getElementById('timer-hint');const defaultTimerHint='Sit with this until the timer ends, then tap Finish Practice to record today.';
function drawTimer(){const m=Math.floor(remaining/60),s=remaining%60;timer.textContent=`${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`}
function resetTimerUI(){toggle.disabled=false;toggle.textContent='Begin';finishBtn.classList.remove('ready');timerHint.textContent=defaultTimerHint}
function stop(){clearInterval(interval);interval=null;running=false;if(remaining>0)toggle.textContent='Begin'}
toggle.addEventListener('click',()=>{if(running){stop();return}running=true;toggle.textContent='Pause';interval=setInterval(()=>{remaining--;drawTimer();if(remaining<=0){stop();toggle.disabled=true;toggle.textContent='Complete';finishBtn.classList.add('ready');timerHint.textContent='Timer complete — tap Finish Practice below.'}},1000)});

document.getElementById('finish-practice').addEventListener('click',async()=>{stop();const check=document.getElementById('primary-check');if(!check.checked){check.checked=true;if(user&&currentStage&&currentPractice){try{setSync('SYNCING…');const duration=(currentPractice.default_minutes||10)*60;const result=await PathBackend.completePractice({stageId:currentStage.id,practiceId:currentPractice.id,durationSeconds:duration});const days=result?.practice_days??progressRow?.practice_days??0;if(progressRow){progressRow.practice_days=days;progressRow.last_practice_date=new Date().toISOString().slice(0,10);progressRow.status=result?.stage_status||progressRow.status}renderCounts(days);document.getElementById('stage-day').textContent=`DAY ${Math.max(1,days+1)}`;if(result?.current_stage_id&&result.current_stage_id!==currentStage.id){await loadRemote()}else{renderPath();renderStageReview();setSync('SYNCED',true)}}catch(e){console.error(e);localState.practiceDays+=1;saveLocal();setSync('LOCAL')}}else{localState.practiceDays+=1;saveLocal()}}resetTimerUI();overlay.classList.add('hidden');const reflect=document.getElementById('today-reflect');if(reflect){reflect.classList.add('invite');setTimeout(()=>reflect.classList.remove('invite'),2600)}});

document.getElementById('journal-form').addEventListener('submit',async e=>{e.preventDefault();const status=document.getElementById('journal-status');if(!window.ASCENDJournalValidation?.hasMeaningfulContent(e.currentTarget)){status.textContent='Write at least one observation before saving this reflection.';e.currentTarget.elements?.namedItem('observation')?.focus();return}const form=new FormData(e.currentTarget),entry={created_at:new Date().toISOString()};for(const [k,v] of form.entries())entry[k]=String(v).trim();if(user&&currentStage){try{setSync('SYNCING…');await PathBackend.saveJournal(user.id,currentStage.id,entry);status.textContent='Reflection saved privately to your ASCEND Path journal.';setSync('SYNCED',true);await refreshMirror()}catch(err){console.error(err);localState.entries.push(entry);saveLocal();status.textContent='Connection unavailable. Reflection saved on this device and can be synchronized later.';setSync('LOCAL')}}else{localState.entries.push(entry);saveLocal();status.textContent='Reflection saved privately on this device. Sign in to synchronize it.'}e.currentTarget.reset()});

document.getElementById('submit-stage-review').addEventListener('click',async()=>{if(!user||!currentStage)return;const status=document.getElementById('review-status');status.textContent='Checking practice history and attainment reflections…';try{setSync('SYNCING…');const result=await PathBackend.submitReadinessReview(currentStage.id);status.textContent=result?.message||'Readiness review submitted.';await loadRemote();setSync('SYNCED',true)}catch(e){console.error(e);status.textContent=e.message||'The stage is not ready for progression yet.';setSync('SYNCED',true)}});
document.getElementById('continue-stage').addEventListener('click',()=>{document.getElementById('review-status').textContent='Continue the current practice. Nothing is lost by taking more time.';nav.find(n=>n.dataset.screen==='today')?.click()});
document.getElementById('refresh-mirror').addEventListener('click',refreshMirror);
document.getElementById('library-search')?.addEventListener('input',event=>{libraryQuery=event.target.value.trim();renderLibrary()});
document.querySelectorAll('.library-filter').forEach(button=>button.addEventListener('click',()=>{libraryType=button.dataset.libraryType||'all';document.querySelectorAll('.library-filter').forEach(item=>item.classList.toggle('active',item===button));renderLibrary()}));

const authStatus=document.getElementById('auth-status');document.getElementById('google-sign-in')?.addEventListener('click',async()=>{const button=document.getElementById('google-sign-in');authStatus.textContent='Opening Google…';button.disabled=true;try{await PathBackend.signInWithGoogle()}catch(err){authStatus.textContent=err.message||'Google sign-in could not start.';button.disabled=false}});
const authForm=document.getElementById('auth-form');authForm?.addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(authForm);authStatus.textContent='Signing in…';try{user=await PathBackend.signIn(f.get('email'),f.get('password'));authStatus.textContent='';showAccount();await loadRemote();await window.ASCENDUX?.syncAuthGate()}catch(err){authStatus.textContent=err.message||'Sign-in failed.'}});
document.getElementById('create-account')?.addEventListener('click',async()=>{const f=new FormData(authForm),email=f.get('email'),password=f.get('password');if(!email||!password){authStatus.textContent='Enter an email and password first.';return}authStatus.textContent='Creating account…';try{const result=await PathBackend.signUp(email,password);authStatus.textContent=result?.access_token?'Account created.':'Account created. Confirm it from the email Supabase sends before signing in.'}catch(err){authStatus.textContent=err.message||'Account creation failed.'}});
document.getElementById('forgot-password')?.addEventListener('click',async()=>{const email=new FormData(authForm).get('email');if(!email){authStatus.textContent='Enter your email first.';return}authStatus.textContent='Sending password reset…';try{await PathBackend.requestPasswordReset(email);authStatus.textContent='Password reset requested. Check your email.'}catch(err){authStatus.textContent=err.message||'Password reset could not be sent.'}});
document.getElementById('toggle-password')?.addEventListener('click',event=>{const input=document.getElementById('account-password'),show=input.type==='password';input.type=show?'text':'password';event.currentTarget.textContent=show?'Hide':'Show';event.currentTarget.setAttribute('aria-pressed',String(show))});
document.getElementById('lifetime-key-form')?.addEventListener('submit',async event=>{event.preventDefault();const input=document.getElementById('lifetime-key'),status=document.getElementById('lifetime-key-status'),button=event.currentTarget.querySelector('button');status.textContent='Checking key…';button.disabled=true;try{const result=await PathBackend.redeemLifetimeKey(input.value);if(result.status==='redeemed'||result.status==='already_redeemed'){status.textContent='Lifetime access is active on this account.';input.value='';await loadRemote()}else if(result.status==='already_used'){status.textContent='This key has already been used by another account.'}else{status.textContent='That key is not valid. Check every letter and number.'}}catch(err){console.error(err);status.textContent=err.message||'The key could not be checked right now.'}finally{button.disabled=false}});
document.getElementById('sign-out').addEventListener('click',()=>{PathBackend.signOut();user=null;entitlement=null;progressRow=null;markerObservations=[];recentJournalText='';window.__pathProgress=[];window.ASCENDProgression?.invalidate?.();document.body.classList.remove('access-required');showAccount();renderCounts(localState.practiceDays);document.getElementById('stage-review-card').classList.add('hidden');document.getElementById('mirror-content').innerHTML='<p>Sign in and begin journaling to create a grounded reflection.</p>';loadRemote()});

function refreshPaywallPrices(){['monthly','annual','lifetime'].forEach(tier=>{const el=document.getElementById(`price-${tier}`),price=window.AscendBilling?.getPriceString(tier);if(el&&price)el.textContent=price})}
window.AscendBilling?.onStatusChange(()=>{refreshPaywallPrices();if(user)loadRemote()});
window.AscendBilling?.init();
setTimeout(refreshPaywallPrices,1500);

document.querySelectorAll('.paywall-buy').forEach(button=>button.addEventListener('click',async()=>{
  const status=document.getElementById('paywall-status');
  button.disabled=true;status.textContent='Opening Google Play…';
  try{await window.AscendBilling.purchase(button.dataset.tier);status.textContent='Verifying your purchase…'}
  catch(err){status.textContent=err.message||'Purchase could not be started.'}
  finally{button.disabled=false}
}));

document.getElementById('paywall-restore')?.addEventListener('click',async event=>{
  const status=document.getElementById('paywall-status'),button=event.currentTarget;
  button.disabled=true;status.textContent='Restoring purchases…';
  try{await window.AscendBilling.restore();status.textContent='Restore complete.';await loadRemote()}
  catch(err){status.textContent=err.message||'Nothing to restore on this account.'}
  finally{button.disabled=false}
});

PathBackend.listenForOAuthCallback(error=>{if(error){authStatus.textContent=error.message||'Google sign-in did not complete.';document.getElementById('google-sign-in').disabled=false;return}location.reload()});
if(!PathBackend.isSignedIn()){document.getElementById('library-list').innerHTML='<div class="empty-state"><h2>Your Library is ready</h2><p>Sign in to load all teachings, practices, readings and references available for your current stage.</p><button class="secondary" type="button" data-go-signin>Sign In</button></div>';document.getElementById('mirror-content').innerHTML='<p>Sign in and begin journaling to create a grounded reflection from your own observations.</p>'}
saveLocal();drawTimer();showAccount();loadRemote();
